import { Alert } from '../../components/Alert';
import { Button } from '../../components/Button';

const Home = () => {
  return (
    <>
      <Alert />
      <Button />
    </>
  );
};

export default Home;
