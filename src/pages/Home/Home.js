import { Alert } from '../../components/Alert';
import { Carousel } from '../../components/Carousel';

const Home = () => {
  return (
    <>
      <Carousel />
      <Alert />
    </>
  );
};

export default Home;
